#include <stdio.h>
#include <malloc.h>

int str_length(char* str);
int size_min_word(char* str);
void print_string(char** str, int size);
void insertion_sort(char** str, int size);
void copy_arr(argc, string, argv);